/** @type {import('next').NextConfig} */
const nextConfig = {
  // Ảnh dự án là URL ngoài (Cloudinary...) và dùng thẻ <img> thường,
  // nên không cần cấu hình next/image domains.
}

export default nextConfig
